# Plan

1. Add range summary calculations to each paginated list (dashboard, libraries, projects, queries).
2. Render "Showing X - Y of Z" in pagination blocks.
3. Update pagination styles to align summary left and controls right.

# Plan

1. Reproduce the navigation issue and identify which link/route is misrouting.
2. Fix route params or link construction so target clicks resolve to `/queries/$targetKey`.
3. Verify sub-target/query links still work and list view remains intact.

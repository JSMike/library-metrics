# Plan

1. Define report metadata and server helpers to list available reports.
2. Add `/reports` and `/reports/$reportId` routes plus dashboard table entry.
3. Implement the framework overview report template with pie chart + table.
